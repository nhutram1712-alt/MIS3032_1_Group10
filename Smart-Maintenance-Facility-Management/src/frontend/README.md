# Frontend

Web UI (Vite + React) cho 4 role: Requester, Technician, Facility Manager, Admin.

## Run

Backend phải chạy trước (mặc định FE proxy tới `http://localhost:5031`).

```bash
cd src/frontend
npm install
npm run dev
```

Mở http://localhost:5173
